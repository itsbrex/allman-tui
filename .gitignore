node_modules/
dist/
*.log
.DS_Store

# Local allman store (per-user message data, has its own git history)
.allman/

# Claude Code local settings (per-user; the shared config is committed)
.claude/settings.local.json
.claude/feedback/

# Release scratch space (scripts/release.ts)
.bin-release/
